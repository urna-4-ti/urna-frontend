const Spinner = () => {
	return (
		<div className="w-full h-1/2 flex justify-center items-center">
			<div className="h-10 w-10 border-[3px] rounded-full border-t-[3px] border-t-primary animate-spin" />
		</div>
	);
};

export default Spinner;
